const inisialState = {

    productos :[],
    error: null,
    loading: false
}
export default function(state = inisialState, action) {

    switch(action.type){

        default: return state;

    }
}